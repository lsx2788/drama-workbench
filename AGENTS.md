# 映序 · 独立重写工程

产品基线只有 `docs/产品需求.md`。实现范围见 `docs/实施边界.md`。用户要求重新建立干净底座，不继承相邻旧工程的数据库、接口、提示词规则或兼容约束。不要根据旧实现补回未确认需求。

当前已进入实际实现：使用独立 SQLite 数据库与原件文件存储，本机 Codex app-server 执行 AI 任务。旧 Mock 仅用于测试，不导入真实数据。上传只保留原件，解析按 AI 或预览请求进行。视频制作与合成由人工处理。

依赖方向：workspace 组装 UI、application 和具体 repository；features 使用 domain/shared；application 只依赖 domain 和公开 HTTP 协议；infrastructure 实现接口；domain 不依赖 UI、存储、网络或演示数据。mocks 的模板不是 AI 输出。

聊天消息、任务操作记录分开。内部审核通过不等于总控验收。禁用审核留下跳过记录；禁用制作仅阻塞依赖缺失产出的分支。代表性制作是默认优先级，不是其他集的硬依赖。资产查询只返回通过项，归档窗口保持空白。

正常制作由系统自动在节点制作 AI 与该节点独立审核 AI 之间转交产出、修改意见和问题；总控只作最终验收/退回与流程推进。编剧决定分集与关键剧集，分镜 AI 决定本集镜头与关键镜头；验收后按实际内容增量展开节点。用户审核入口是按需介入，不是必经步骤；人工介入默认收起，必须记录真实操作身份和理由，不能冒充 AI 自动审核。

只复用经检查适用的模块，不整库搬运。不要引入旧页面、全局样式或旧数据库；仅可复用检查过的隔离通信模块。源文件不得包含用户媒体、凭据、数据库或原始聊天导出。当前是单机单拥有者服务，不承诺完整团队权限或多实例部署。

修改后运行 `npm test`、`npm run typecheck`、`npm run build`；界面改动检查桌面和手机。Next.js 使用本机已安装版本，改路由/布局前阅读 `node_modules/next/dist/docs/` 对应指南。

资产审核授权例外：有已验收首样的补充图片，经对应资产审核 AI 审核后，由系统校验权限、版本、文件、依赖与未决问题并记录 reviewer 的最终验收。首样、无基准图片、镜头画面和整包产出仍由总控验收。以 docs/产品需求.md 的 2026-09-20 更新为准。
