# Contributing

映序目前处于早期开发阶段，欢迎提交可复现的问题与小范围改进。

## 开发和验证

使用 Node.js 24+，运行 `npm ci`。启动方式与本机 Codex 前置条件见 README。
提交前运行 `npm test`、`npm run typecheck` 和 `npm run build`。界面改动请检查桌面和手机布局。

请描述问题、预期行为、实际行为和复现步骤。PR 应说明改动及验证结果。
遵守 AGENTS.md 中的模块边界；审核通过、最终验收和用户确认是不同状态，不能相互冒充。

## 数据和安全

不要提交数据库、原始聊天、用户小说、图片或视频素材、账户登录文件、API 密钥和服务器配置。
测试使用临时目录和虚构数据，不访问维护者的正式制作数据。
报告问题前删除日志中的个人信息、令牌和本地私人路径。

本项目是单机、单拥有者服务。若需要远程访问，应自行提供可信的身份认证和传输保护。
不要直接把无认证的服务端口暴露到公网。

## License

代码按 MIT 许可分发，见 LICENSE。用户自行导入的素材和第三方模型、服务不包含在本项目的许可中。
