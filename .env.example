DATA_DIR=./data
# Optional native Codex executable; defaults to PATH. Log in through Codex with ChatGPT.
CODEX_BIN=
OPENAI_API_KEY=
OPENAI_MODEL=
OPENAI_IMAGE_MODEL=
# Optional API protection when accessed outside localhost. First release binds localhost.
WORKBENCH_TOKEN=
